name: Bootstrap NEXUS (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap completed; this workflow is retained only for audit history."
